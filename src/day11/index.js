const parseInput = input => parseInt(input.trim(), 10);

const moduleA = input => {
  return parseInput(input);
};

const moduleB = input => {
  return parseInput(input);
};

export {
  moduleA,
  moduleB,
};
